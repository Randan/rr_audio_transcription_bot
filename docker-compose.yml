services:
  app:
    build: .
    container_name: audio-transcription-bot
    env_file: .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
